# ---- Core ML stack ----
# 建议锁定 torch 版本以匹配 lightning 2.2.4，避免安装到不稳定的最新版
# 注意：具体的 torch url (cu118/cu121) 通常需要在 pip install 命令中指定，
# 但在 requirements 中锁定版本号是第一步。
torch==2.1.2
torchvision==0.16.2
torchaudio==2.1.2

transformers==4.40.2
tokenizers>=0.19.0
sentencepiece
lightning==2.2.4
accelerate
# Chemprop/Lightning 训练日志依赖
tensorboard

# ---- Scientific stack ----
# 必须保持 < 2.0.0 以兼容 chemprop 和 rdkit，1.26.4 是最佳选择
numpy==1.26.4
# 稍微升级 scipy 以更好适配 Py3.11，或者保持 1.10.1 也可以
scipy==1.11.4
pandas==2.1.4
scikit-learn==1.7.2
chemprop==2.2.1

# ---- Chemistry / Graph ----
networkx==3.2.1
rdkit==2023.09.1

# ---- Utilities ----
tqdm==4.66.2
joblib==1.3.2
requests==2.32.3
# 建议添加，用于更快的安装解析（可选）
packaging